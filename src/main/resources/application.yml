spring:
  datasource:
    driverClassName: org.postgresql.Driver    
    url: jdbc:postgresql://134.209.121.60/sigap
    username: postgres
    password: sig@p$$

server:
   port: 8080
   
eureka:
  client:
    serviceUrl:
      defaultZone: https://modulo-alumno-eureka.herokuapp.com/eureka #http://localhost:8761/eureka/
  instance:
    hostname: localhost
    nonSecurePort: 8080

banner:
  location: classpath:/example/banner.txt